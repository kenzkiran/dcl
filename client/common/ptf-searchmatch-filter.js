module.exports = function () {    return function (input, searchText, searchCategory) {        searchText = searchText || '';        var output = [];        for (var i = 0; i < input.length; ++i) {            var item = input[i];            var t1 = item.teamOne.name.toLowerCase();            var t2 = item.teamTwo.name.toLowerCase();            var u1 = item.umpireOne.name.toLowerCase();            var u2 = item.umpireTwo.name.toLowerCase();            var search = searchText.toLowerCase();            var isInTeam = t1.indexOf(search) > -1 || t2.indexOf(search) > -1;            var isInUmpire = u1.indexOf(search) > -1 || u2.indexOf(search) > -1;            var shouldAdd = false;            if (searchCategory === 'both') {                shouldAdd = isInTeam || isInUmpire;            } else if (searchCategory === 'team') {                shouldAdd = isInTeam;            } else {                shouldAdd = isInUmpire;            }            if (shouldAdd) {                output.push(item);            }        }        return output;    };};